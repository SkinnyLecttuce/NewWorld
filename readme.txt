Clone tips:

Run "pip install -r requirements.txt"

Edit /apps/services/api_key.txt with a valid API key

To test if things are working correctly run /testing/Testing_Integration.py

Latest wordlist.json is downloaded automatically. If you need to reinstall wordlist.json, empty the file /apps/services/log.txt


